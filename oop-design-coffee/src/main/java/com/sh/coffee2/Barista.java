package com.sh.coffee2;

public class Barista {
    private int stock = 1000; // 커피재고 그램(g)

    public Coffee[] makeCoffee(Order order) {
//        Coffee[] coffees = new Coffee[order.getCount()];
//        for (int i = 0; i < coffees.length; i++) {
//            this.stock -= 20;
//            coffees[i] = new Coffee(order.getMenuName(), order.getTemperatureType());
//        }
//        return coffees;
        return null;
    }
}
